[package]
name = "command-engine"
version = "1.0.0-dev"
authors = ["FssAy"]
edition = "2018"

[lib]
path = "src/lib.rs"

[dependencies.tokio]
version = "1.*"
features = [
    "rt-multi-thread",
    "sync",
]
optional = true

[features]
default = ["engine"]
async = ["tokio"]
engine = []
